name = "mirai-web-cad"
compatibility_date = "2026-08-22"
pages_build_output_dir = "dist"

[vars]
APP_ENV = "preview"
# Pages(preview含む)はAccessの外側の公開オリジンなので、認証はaccess固定にする。
# 未設定だとdemo認証(ヘッダー自己申告)へフォールバックする事故につながるため明示する。
AUTH_MODE = "access"
