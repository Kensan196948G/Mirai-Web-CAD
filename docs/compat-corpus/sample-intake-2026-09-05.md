# DXFサンプル受入・回帰検証

2026-09-05、ユーザー提供の`sample/DXF_sample_set.zip`をCRC検証し、通常ファイル8件・展開サイズ22,439 bytes・危険なパスやシンボリックリンクなしを確認して`sample/DXF_sample_set/`へ展開した。ZIPと展開図面はGit対象外とする。開発・検証への利用指示は受領済みだが、公開再配布の許諾や実案件由来は確認していない。

## 内容と結果

全7図面はAC1009 (R12)のテキストDXF。付属READMEの単位指定はmmだが、DXFヘッダーにINSUNITSはない。単位の往復保持を認定するデータではない。

| 図面 | 図形数 | レイヤー数 | 修正後の内部往復回帰 |
| --- | ---: | ---: | --- |
| 道路平面図 | 25 | 7 | 合格 |
| 擁壁 | 42 | 6 | 合格 |
| 仮設ヤード | 28 | 7 | 合格 |
| 橋脚・杭配置 | 31 | 6 | 合格 |
| 断面図 | 12 | 6 | 合格 |
| 施工計画図 | 33 | 7 | 合格 |
| CAD機能試験用複雑図面 | 57 | 7 | 合格 |

合計228図形: LINE 109、POLYLINE 40、CIRCLE 33、ARC 5、TEXT 41。初回取込・書出し・再取込の警告/スキップは全7件で0。

初回は施工計画図と複雑図面が不合格。比較器が円の中心のみで距離を計算し、半径の異なる同心円を同率候補としたため。円の半径差を対応付け距離へ追加し、ID再採番・順序変更・半径変更検出・同一半径の曖昧性保持を回帰テスト化した。許容差や合格閾値は緩和していない。

## 再実行

```bash
node scripts/check-dxf-samples.mjs
# 別のローカルデータを検証する場合
node scripts/check-dxf-samples.mjs /path/to/extracted-dxf artifacts/dxf-samples
```

直下のDXFを全件検証し、SHA-256、元図形種別/数、レイヤー数、DXF版、往復診断を`artifacts/dxf-samples/report.json`へ生成する。図面内容を含み得るためレポートもローカル保持。失敗が1件でもあれば終了コード1、空フォルダも失敗とする。UTF-8/ASCII以外は文字化けを避けるため拒否し、Shift-JIS判定は後続。

## 認定範囲外

- 比較の期待値は初回インポート後の内部モデル。初回取込で失われた属性まで検出する原本DXFとの完全比較ではない。`pass90`はこの限定回帰の採点であり、製品の90%代替率ではない。
- DIMENSION、HATCH、INSERT/BLOCK、ATTDEF/ATTRIB、MTEXT、ELLIPSE、SPLINE、Layout/Viewportのサンプルはない。寸法はLINE/TEXTへ簡略化されている。
- 外部CADでの再読込、原本との描画比較、フォント、日本語文字、尺度保証印刷、100k性能は未認定。
- 実案件100図面台帳には加算しない。開発サンプル7件と実案件受入認定は区別する。今後は不足するエンティティ・属性を含むDXFと、正解図/PDF・単位・利用許諾をセットで収集する。
